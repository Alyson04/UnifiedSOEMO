body {
    background: linear-gradient(to right, #CDFFD8 40%, #94B9FF 70%);
    color: #2A4365;
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Inter', sans-serif;
}
.container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
}
/* Navbar */
.navbar {
    background: linear-gradient(to right, #CDFFD8 40%, #94B9FF 70%);
    color: white;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1px 5px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
}

.logo img {
    height: 50px;
}

.search-profile {
    display: flex;
    align-items: center;
    gap: 15px;
}

.search-profile input {
    padding: 8px 35px;
    border-radius: 20px;
    border: none;
    outline: none;
    background-color: white;
    font-size: .8em;
    font-style: italic;
}

.search-profile img {
    width: 24px;
    height: 24px;
}

.profile {
    display: flex;
    align-items: center;
    gap: 10px;
}

.profile img {
    width: 50px;
    height: 50px;
    border-radius: 50%;
    object-fit: cover;
}

.profile-text {
    display: flex;
    flex-direction: column;
}

.profile-text span {
    font-size: 16px;
    font-weight: bold;
    color: #333;
    text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.3);
}

.profile-text p {
    font-size: 14px;
    color: #666;
    margin: 0;
}


.nav-list {
    list-style: none;
    display: flex;
    justify-content: center;
    margin: 0;
    padding: 0;
  }
  
  .nav-list li {
    margin: 0 25px;
  }
  
  .nav-list a {
    color: #2A4365;
    text-decoration: none;
    font-size: 18px;
    font-weight: bold;
    transition: color 0.3s;
  }
  
  .nav-list a:hover {
    color: #1abc9c;
  }
  
 /* General layout */
.about-wrapper {
    font-family: 'Inter', sans-serif;
    padding: 0 5%;
}

/* Section Titles */
.section-title {
    font-size: 26px;
    font-weight: 800;
    color: white;
    background-color: #2A4365;
    text-align: center;
    padding: 10px 0;
    margin: 30px 0 15px;
}

/* ABOUT US */
.about-us-section {
    background: linear-gradient(to right, #e0ffff, #c6d8ff);
    border-radius: 8px;
    display: flex;
    flex-direction: column;
    padding: 20px;
    box-shadow: 0 5px 10px rgba(0,0,0,0.1);
}

.about-us-content {
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
}

.about-text-box {
    flex: 1;
    padding: 20px;
    font-size: 1.05rem;
    line-height: 1.7;
    color: #2A4365;
}

.about-image-box {
    flex: 1;
    text-align: center;
}

.about-image-box img {
    width: 100%;
    max-width: 400px;
    height: auto;
    border-radius: 10px;
}

/* MISSION and VISION */
.mission-vision-section {
    background-color: #2A4365;
    padding: 20px;
    border-radius: 8px;
    margin-top: 20px;
}

.mission-vision-grid {
    display: flex;
    flex-direction: row;
    gap: 20px;
    flex-wrap: wrap;
    justify-content: space-between;
}

.mission-box, .vision-box {
    flex: 1;
    background: linear-gradient(to right, #ccf5e7, #cce5ff);
    padding: 20px;
    border-radius: 10px;
    min-width: 280px;
    color: #2A4365;
    box-shadow: 0 4px 8px rgba(0,0,0,0.1);
}

.mission-box h3, .vision-box h3 {
    margin-bottom: 10px;
    font-size: 1.2rem;
}

/* CONTACT US */
.contact-us-section {
    background: linear-gradient(to right, #ccf5e7, #cce5ff);
    padding: 20px;
    margin-top: 20px;
    border-radius: 10px;
}

.contact-content {
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    gap: 30px;
    align-items: center;
}

.contact-text-box {
    flex: 1;
    color: #2A4365;
    font-size: 1rem;
    line-height: 1.6;
}

.contact-image-box {
    flex: 1;
    text-align: center;
}

.contact-image-box img {
    width: 300%;
    max-width: 500px;
    height: 350px;
    border-radius: 10px;
}

.notification-modal {
  position: absolute;
  top: 40px;
  right: 0;
  width: 300px;
  max-height: 400px;
  background-color: white;
  border: 1px solid #ccc;
  border-radius: 8px;
  box-shadow: 0px 2px 10px rgba(0, 0, 0, 0.2);
  overflow-y: auto;
  z-index: 100;
  padding: 10px;
}

.notification-modal.hidden {
  display: none;
}

.notification-header {
  font-weight: bold;
  margin-bottom: 10px;
  font-size: 16px;
  border-bottom: 1px solid #ddd;
  padding-bottom: 5px;
}

.notification-list p {
  padding: 8px 0;
  border-bottom: 1px solid #f0f0f0;
  font-size: 14px;
  color: #333;
}

.notification-list p:last-child {
  border-bottom: none;
}

/* Mobile Responsive Styles */
@media (max-width: 768px) {
    .about-wrapper {
        padding: 60px 15px 0;
    }

    .section-title {
        font-size: 22px;
        margin: 20px 0 10px;
    }

    .about-us-content {
        flex-direction: column;
        gap: 20px;
    }

    .about-text-box {
        padding: 15px;
        font-size: 1rem;
        text-align: center;
    }

    .about-image-box {
        order: -1;
    }

    .about-image-box img {
        max-width: 100%;
    }

    .mission-vision-grid {
        flex-direction: column;
        gap: 15px;
    }

    .mission-box, .vision-box {
        min-width: auto;
        padding: 15px;
    }

    .contact-content {
        flex-direction: column;
        gap: 20px;
    }

    .contact-text-box {
        padding: 15px;
        text-align: center;
    }

    .contact-image-box img {
        max-width: 100%;
    }
}

@media (max-width: 480px) {
    .about-wrapper {
        padding: 70px 15px 0;
    }

    .section-title {
        font-size: 20px;
        margin: 15px 0 10px;
    }

    .about-text-box,
    .mission-box,
    .vision-box,
    .contact-text-box {
        font-size: 0.95rem;
    }
}
